<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.kush.learningkotlin">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name="whtsAppClone.Chat_Login"/>
        <activity android:name="whtsAppClone.MainWhtsappActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".learningRandomStuff.data.mainAppFiles.ui.login.LoginActivity"/>
        <activity android:name=".learningRandomStuff.data.mainAppFiles.MainKotlinActivity"/>
        <activity android:name=".learningRandomStuff.data.ListView" />
        <activity android:name=".learningRandomStuff.data.MainActivity" />
    </application>

</manifest>